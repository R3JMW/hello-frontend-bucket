import { TestPopupDirective } from './test-popup.directive';

describe('TestPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new TestPopupDirective();
    expect(directive).toBeTruthy();
  });
});
